import { View, Text } from "react-native";
import React from "react";

const RoundGroupCard = ({ group }) => {
  return (
    <View>
      <Text>roundGroupCard</Text>
    </View>
  );
};

export default RoundGroupCard;
